*.o
*.a
*.bin
*.img
*.iso
*.map
*.log

# Screenshots are checked in: the README links to them, and they are produced
# by tools/shot.py rather than by hand, so they are build output that is worth
# keeping under review.
!shots/*.png
iso/
build/
.vscode/
.idea/
.DS_Store
*.swp
*.tmp

# Windows Explorer drops these alternate-data-stream files all over a WSL
# share; they are not part of the project.
*:Zone.Identifier

# Prebuilt Windows binary shipped in the vendored BearSSL tree. hawkOS does
# not use the T0 compiler - the generated .c files are already in src/.
third_party/bearssl/T0Comp.exe

# Scratch file used by tools/patch.py; contents are transient.
tools/edits.json

# The layout test corpus: megabytes of somebody else's markup, which also goes
# stale. tools/pages/fetch.sh rebuilds it, and the regressions that matter are
# pinned by the committed fixtures in tools/layout/fixtures.
tools/pages/*.html

# Working files from a screenshot session (crops, throwaway captures).
shots/_*.png
